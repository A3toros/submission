{
  "name": "netlyfi-neondb",
  "version": "1.0.0",
  "description": "Netlify + NeonDB submission site",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}